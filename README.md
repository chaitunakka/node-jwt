# Node JWT example with jsonwebtoken

## Quick Start

```bash
# Install dependencies
npm install

# Serve on localhost:3000
npm start

# Get token with default user
http://localhost:3000/api/auth

# Send token
http://localhost:3000/api/posts
```
